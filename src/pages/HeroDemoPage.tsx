import React from 'react';
import { HeroDemo } from '../components/ui/demo';

const HeroDemoPage: React.FC = () => {
  return (
    <div className="min-h-screen bg-gradient-to-b from-gray-50 to-white">
      <HeroDemo />
    </div>
  );
};

export default HeroDemoPage;
